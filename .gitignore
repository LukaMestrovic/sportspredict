# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local data / scratch
/tmp/
*.log
out/
# cron submission audit trail (runtime, per-machine)
logs/cron_submissions.jsonl

# paid odds-api cache
cache/
